load "msh3"     // generate 3D meshes 
load "medit"   // visualization 
load "gmsh"   // msh export 
//load "iovtk"   === to export .vtu files 



real L = 2.0;
real h = 0.1;
real b = 0.1;

mesh3 Th = cube(50, 5, 5, [L*x, h*y, b*z]);
//medit("maillage", Th, wait=1);  // visualization with "medit"


// savemesh(Th, "beam.mesh"); // ce format n'est pas vraiment interessant 
savegmsh(Th, "beam");